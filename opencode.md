20260814

# 开发流程规则
本规则描述"先设计、先计划、确认后执行、执行后验证"的通用工作方式；本规则不依赖任何特定工具，若有开发流程工具可用，其执行编排须与本规则一致。

## spec（设计文档）
1.大版本开发任务必须先编写spec并落盘，spec经老板确认后方可进入plan阶段；
2.增量修改（修bug等小修小补）不需要spec，直接进入plan；
3.spec一律落盘到当前项目目录下的specs目录，文件名为版本号.日期.功能.spec；目录扁平，不建立深层子目录。

## plan/build 模式规则（plan格式）
1.先把plan落盘；开发任务（有git仓库的项目）的plan编制、执行与验证必须走完整的spec→plan→执行→验证→评审流程；plan一律落盘到当前项目目录下的plans目录，文件名为版本号.日期.功能.plan（这个版本号是大版本号）；大版本与增量修改落盘位置相同；
2.plan的开头写上执行增量git，前git，commit参考最高规则5.6；
3.git命令之后是详细plan，包括要修改/删除/新增的文件名和行号，内容；
4.修改之后要写执行增量git，后git，commit参考5.6；
5.最后写上，只能执行计划内的修改，计划外的内容，一个字符都不能修改；
6.如果执行时发现计划不成熟，要停下来向老板提出，问是否需要回退，然后修改计划重新执行；老板同意后返回plan模式，按上述1到4重新写计划，再进入build模式执行，不允许做计划外的修改；
7.build模式执行修改之后，要根据修改前的git、落盘的plan文件以及本次修改的代码做比对，看是否执行了计划外的修改，并把结果报告老板；
8.plan经老板确认落盘后，即视为对该plan范围内所有写执行的显式授权；执行阶段若当前环境提供开发流程工具，则由其流程编排并完成验证，否则按plan逐条执行并验证；超出plan范围的任何修改，必须停下向老板请示，不得自行执行。
