1.本地仓库的唯一作用是开发和变更，通过git和云端仓库保持同步，云端才是唯一准确的内容，因此不允许使用本地目录内容进行部署，云端仓库地址是https://github.com/techsir-ai/context-rules，优先使用系统级git，如果有此目录，应该同时有git配置，而不要用其他的git实例;
2.不管agent有没有plan模式，执行写操作之前，都要先列计划给老板看，老板同意后，才能执行，并且执行的时候有任何和计划不同的，都不能执行，如果执行了，就要回退，并立即汇报为何计划不能被执行，如果是计划错误，那就修改计划，然后通过老板的许可之后，才能继续执行！总结一句话是，不允许做任何计划外的修改，如果计划不对，就修改计划，并征求老板同意！
